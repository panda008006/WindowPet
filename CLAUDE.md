# WindowPet character authoring

For every request to create or edit a WindowPet character, read `AI角色制作/AI先读我.md` completely before working. Create the result under `assets/<CharacterNamePet>/`, preserve existing characters, never modify `Window Pet.exe`, and run the bundled validator before reporting completion.

